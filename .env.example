# Example env vars

# Network name used by facilitator (e.g. base, base-sepolia, solana)
X402_NETWORK=base
# USDC token address on the selected network (use Sepolia test address when testing)
# USDC token address on the selected network (server-side authoritative).
# The server will include this in `paymentRequirements.tokenAddress` when returning a 402.
# If you want the client to know a fallback at build time, set the NEXT_PUBLIC var below.
USDC_BASE_ADDRESS=0xUSDC_PLACEHOLDER
# Receiving address for service payments. Set this to your wallet address in production.
SERVICE_RECIPIENT_ADDRESS=0xSERVICE_RECIPIENT_PLACEHOLDER
# Price as decimal USDC string (human-friendly). Convert to raw token units when sending on-chain.
PRICE_IN_USDC=0.0001

# Optional: agent private key for local agent testing (never commit a real key)
# AGENT_PRIVATE_KEY=0x...

# Optional client-side env (public): if set, the client can use this as a fallback token address
# for building on-chain transfers. Prefer the server-provided `paymentRequirements.tokenAddress`.
NEXT_PUBLIC_USDC_BASE_ADDRESS=0xUSDC_PLACEHOLDER

 # Local facilitator API key for /api/payments/* endpoints
LOCAL_FACILITATOR_API_KEY=changeme-local-facilitator-key

# Legacy facilitator settings (no longer used by default)
# X402_FACILITATOR_URL=
# NEXT_PUBLIC_X402_FACILITATOR_URL=

ENABLE_EIP3009= # Optional: enable EIP-3009 transfer with authorization support (default: false)
NEXT_PUBLIC_ENABLE_EIP3009= # Optional: enable EIP-3009 transfer with authorization support on client (default: false)
SETTLEMENT_PRIVATE_KEY=0xSETTLEMENT_PRIVATE_KEY_PLACEHOLDER